import React from 'react'

export const User = (props) => {
  const styles = getStyles()

  return (
    <div style={styles.container}>
      <h1>User</h1>
    </div>
  )
}

const getStyles = () => ({
  container: {},
})
